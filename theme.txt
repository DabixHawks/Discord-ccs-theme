/**
 * @name Hellva boss theme
 * @author DabixHawks
 * @description First ever theme idk if it will work or not;/
 * @version 0.0.1
 */
:root {
  --bg-overlay: rgba(10, 5, 20, 0.85);
  --bg-secondary: #160b2e;
  --accent-red: #e83b3b;
  --accent-pink: #ff3cac;
  --accent-blue: #3cf2ff;
  --text-main: #f5f5f5;
  --text-muted: #b8a9d9;
  --glow: 0 0 14px rgba(255, 60, 172, 0.7);
}

/* === MAIN BACKGROUND === */
body {
  background:
    linear-gradient(var(--bg-overlay), var(--bg-overlay)),
    url("https://drive.google.com/file/d/1QAmdR-mVcgWfHXeAjNcIXCWKSzk8sBtG/view?usp=drive_link");
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
  color: var(--text-main);
  font-family: 'Segoe UI', sans-serif;
}

/* Panels / Cards */
.container, .card, .panel {
  background: linear-gradient(
    145deg,
    rgba(22, 11, 46, 0.95),
    rgba(12, 6, 30, 0.95)
  );
  border: 1px solid rgba(255, 60, 172, 0.3);
  border-radius: 16px;
  box-shadow: var(--glow);
  backdrop-filter: blur(6px);
}

/* Headings */
h1, h2, h3 {
  color: var(--accent-pink);
  text-shadow: 0 0 8px rgba(255, 60, 172, 0.9);
}

/* Buttons */
button {
  background: linear-gradient(
    135deg,
    var(--accent-red),
    var(--accent-pink)
  );
  color: white;
  border: none;
  border-radius: 14px;
  padding: 10px 18px;
  box-shadow: 0 0 12px rgba(232, 59, 59, 0.7);
  transition: all 0.2s ease;
}

button:hover {
  transform: scale(1.06);
  box-shadow: 0 0 22px rgba(255, 60, 172, 1);
}

/* Links */
a {
  color: var(--accent-blue);
  text-shadow: 0 0 6px rgba(60, 242, 255, 0.7);
}

a:hover {
  color: var(--accent-pink);
}

/* Scrollbar */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-thumb {
  background: linear-gradient(
    var(--accent-red),
    var(--accent-pink)
  );
  border-radius: 10px;
}
